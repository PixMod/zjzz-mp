const SERVER_HOST = 'https://pixos.dpdns.org';

const ASSETS_HOST = 'https://assets.pixos.dpdns.org';
const ICON_PATH = `${ASSETS_HOST}/icons`;
const IMAGE_PATH = `${ASSETS_HOST}/images`;
const CONFIG_PATH = `${ASSETS_HOST}/configs`;

module.exports = {
  SERVER_HOST,
  ASSETS_HOST,
  ICON_PATH,
  IMAGE_PATH,
  CONFIG_PATH
};